version: '3.8'

services:
  nexus-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://cryptantus.com/api
    ports:
      - "80:80"
      - "443:443"
    restart: always
    container_name: nexus-pay-frontend
